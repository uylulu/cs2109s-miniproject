# In my submission

- The ciphertext_model.zip: contains code for training a model and using it to decode ciphertexts. The code used in my final submission is in the file: ciphertext_decoder.py 

- The image_and_direction_classification.zip: contains code for training and classifying images into corresponding labels. And also training a model to classify the direction of entities with a direction on top of them. The final code used in my agent submission are in classification_lib/ directory. 

- My training data is in data.zip file

- All code used to train and load data are in the *.ipynb files.


